# Installation

Install this project with either `npm` or `yarn`:

```bash
npm install
# or
yarn
```

# Run project:

Run server by json-server:

```bash
npm run server
# or
yarn server
```

```bash
npm run start
# or
yarn start
```
